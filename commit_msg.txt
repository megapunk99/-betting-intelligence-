Overhaul live odds, ensemble stacking, overfitting detection, and bet journal

10 critical pipeline fixes:

ISSUE 1 - Live odds integration: LiveDataGateway.get_live_odds_dataframe() with direct
TheOddsAPI v4 calls + 10-min cache. EdgeDetector.detect_all() accepts live_market_df.
Removed hardcoded 220.0 market_total fallback.

ISSUE 2 - Removed duplicate --live line from main.py argparse epilog.

ISSUE 3 - Bankroll simulation: decimal_odds now read from each bet record instead of
hardcoded 1.91. Added decimal_odds column to backtesting bet records.

ISSUE 4 - Ensemble builder now trains Ridge regression meta-learner with TimeSeriesSplit
on out-of-fold predictions instead of simple average.

ISSUE 5 - Overfitting detector: _check_overfitting now computes train/test metrics from
real per-fold fold_metrics (tracked in walk-forward loop).

ISSUE 6 - Feature pipeline: _build_tomorrow_feature_vector stores FeatureEngineer pipeline
for consistent train/inference feature computation.

ISSUE 7 - Recommendation engine: generate_all_bets() accepts predictions DataFrame and
uses ML pipeline predictions as signal source.

ISSUE 8 - Renamed shadowing variable 'pd' to 'prop_dict' in generate_player_props.

ISSUE 9 - 429/quota error handling in _load_live_data: quota errors raise RuntimeError,
other errors fall back to historical data.

ISSUE 10 - BetJournal: new services/journal.py with SQLite-backed persistence for bet
history tracking, P&L aggregation, and PerformanceTracker integration.
