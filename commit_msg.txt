Set up daily automated NBA pipeline (Windows Task Scheduler)

- Add tools/daily_run.py — wrapper that scrapes new games via
  update_data.py, runs forward_test.py --calibrated, and logs
  results to logs/daily_YYYY-MM-DD_HHMMSS.log
- Add run_daily.bat — batch entry point for the scheduled task
- Add logs/.gitkeep — keeps log directory in git
- Add ODDS_API_KEY to .env for automated access
- Fix forward_test.py: stop dropping rows with NaN features
  (backfill handles this now)
- Schedule: NBA\DailyPipeline runs daily at 09:00 via schtasks
