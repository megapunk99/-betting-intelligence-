# ═══════════════════════════════════════════════════════════════════════════
# Betting Intelligence System — Python Dependencies
# ═══════════════════════════════════════════════════════════════════════════
#
# Install with:  pip install -r requirements.txt
# Or use the install.ps1 script on Windows.
#
# These are pinned from pyproject.toml. For development, use:
#   pip install -e ".[dev,dashboard]"
# ═══════════════════════════════════════════════════════════════════════════

# ── Core Data Science ─────────────────────────────────────────────────────
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.11
statsmodels>=0.14
joblib>=1.3

# ── Machine Learning Models ───────────────────────────────────────────────
xgboost>=2.0
lightgbm>=4.0

# ── Database ──────────────────────────────────────────────────────────────
SQLAlchemy>=2.0
alembic>=1.13
aiosqlite>=0.19
psycopg2-binary>=2.9

# ── API & Web ─────────────────────────────────────────────────────────────
fastapi>=0.109
uvicorn[standard]>=0.27
jinja2>=3.1
aiofiles>=23.2
python-multipart>=0.0.6

# ── HTTP & Networking ─────────────────────────────────────────────────────
httpx>=0.26

# ── Configuration & CLI ───────────────────────────────────────────────────
click>=8.1
python-dotenv>=1.0
pydantic>=2.5
pydantic-settings>=2.1

# ── Logging & Monitoring ──────────────────────────────────────────────────
loguru>=0.7
prometheus-client>=0.19

# ── Dashboard (optional — install separately if needed) ───────────────────
# streamlit>=1.28
# plotly>=5.18

# ── Development (optional — install separately if needed) ─────────────────
# pytest>=8.0
# pytest-cov>=4.1
# pytest-asyncio>=0.23
# ruff>=0.3
